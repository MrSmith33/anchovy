module templatemanager;

import anchovy.gui.all;

class TemplateManager
{
	void parseTemplate(string filename)
	{

	}

	Widget createTemplatedWidget(Widget widget, string templateName)
	{
		assert(false);
	}
}